/// UI Layout constants for consistent spacing, sizing, and typography
/// All padding, margin, gap, width, height, and font size values are centralized here
library;

// ============================================================================
// SPACING CONSTANTS
// ============================================================================

/// Extra small spacing (2px)
const double kSpaceXS = 2.0;

/// Small spacing (5px)
const double kSpaceS = 5.0;

/// Small-medium spacing (8px)
const double kSpaceSM = 8.0;

/// Medium spacing (10px)
const double kSpaceM = 10.0;

/// Medium-large spacing (12px)
const double kSpaceML = 12.0;

/// Large spacing (16px)
const double kSpaceL = 16.0;

/// Extra large spacing (20px)
const double kSpaceXL = 20.0;

/// 2X large spacing (24px)
const double kSpace2XL = 24.0;

/// 3X large spacing (30px)
const double kSpace3XL = 30.0;

/// 4X large spacing (48px)
const double kSpace4XL = 48.0;

// ============================================================================
// PADDING CONSTANTS
// ============================================================================

/// Extra small padding (all sides)
const double kPaddingXS = 2.0;

/// Small padding (all sides)
const double kPaddingS = 5.0;

/// Small-medium padding (all sides)
const double kPaddingSM = 8.0;

/// Medium padding (all sides)
const double kPaddingM = 10.0;

/// Medium-large padding (all sides)
const double kPaddingML = 12.0;

/// Large padding (all sides)
const double kPaddingL = 16.0;

/// Extra large padding (all sides)
const double kPaddingXL = 20.0;

/// 2X large padding (all sides)
const double kPadding2XL = 24.0;

// ============================================================================
// MARGIN CONSTANTS
// ============================================================================

/// Small margin
const double kMarginS = 5.0;

/// Medium margin
const double kMarginM = 10.0;

/// Large margin
const double kMarginL = 16.0;

/// Extra large margin
const double kMarginXL = 20.0;

// ============================================================================
// BORDER RADIUS CONSTANTS
// ============================================================================

/// Small border radius
const double kBorderRadiusS = 4.0;

/// Medium border radius
const double kBorderRadiusM = 8.0;

/// Large border radius
const double kBorderRadiusL = 12.0;

/// Extra large border radius
const double kBorderRadiusXL = 16.0;

/// Circular border radius (for buttons, avatars)
const double kBorderRadiusCircular = 1000.0;

// ============================================================================
// FONT SIZE CONSTANTS
// ============================================================================

/// Extra small font size (12px)
const double kFontSizeXS = 12.0;

/// Small font size (14px)
const double kFontSizeS = 14.0;

/// Medium font size (16px)
const double kFontSizeM = 16.0;

/// Medium-large font size (18px)
const double kFontSizeML = 18.0;

/// Large font size (20px)
const double kFontSizeL = 20.0;

/// Extra large font size (24px)
const double kFontSizeXL = 24.0;

/// 2X large font size (32px)
const double kFontSize2XL = 32.0;

/// 3X large font size (40px)
const double kFontSize3XL = 40.0;

/// 4X large font size (45px)
const double kFontSize4XL = 45.0;

/// 5X large font size (50px)
const double kFontSize5XL = 50.0;

// ============================================================================
// ICON SIZE CONSTANTS
// ============================================================================

/// Small icon size
const double kIconSizeS = 16.0;

/// Medium icon size
const double kIconSizeM = 24.0;

/// Large icon size
const double kIconSizeL = 32.0;

/// Extra large icon size
const double kIconSizeXL = 48.0;

// ============================================================================
// BUTTON SIZE CONSTANTS
// ============================================================================

/// Button height - small
const double kButtonHeightS = 36.0;

/// Button height - medium
const double kButtonHeightM = 48.0;

/// Button height - large
const double kButtonHeightL = 56.0;

/// Button minimum width
const double kButtonMinWidth = 88.0;

/// Button padding horizontal
const double kButtonPaddingH = 16.0;

/// Button padding vertical
const double kButtonPaddingV = 8.0;

// ============================================================================
// CONTAINER SIZE CONSTANTS
// ============================================================================

/// Small container size
const double kContainerSizeS = 100.0;

/// Medium container size
const double kContainerSizeM = 120.0;

/// Large container size
const double kContainerSizeL = 140.0;

/// Extra large container size
const double kContainerSizeXL = 160.0;

// ============================================================================
// STROKE/BORDER WIDTH CONSTANTS
// ============================================================================

/// Thin stroke width
const double kStrokeWidthThin = 1.0;

/// Medium stroke width
const double kStrokeWidthMedium = 2.0;

/// Thick stroke width
const double kStrokeWidthThick = 4.0;

/// Extra thick stroke width
const double kStrokeWidthXThick = 8.0;

// ============================================================================
// OPACITY CONSTANTS
// ============================================================================

/// Disabled opacity
const double kOpacityDisabled = 0.38;

/// Medium opacity
const double kOpacityMedium = 0.6;

/// Light opacity
const double kOpacityLight = 0.87;

/// Full opacity
const double kOpacityFull = 1.0;

// ============================================================================
// ANIMATION DURATION CONSTANTS
// ============================================================================

/// Fast animation duration (100ms)
const int kAnimationDurationFast = 100;

/// Medium animation duration (300ms)
const int kAnimationDurationMedium = 300;

/// Slow animation duration (500ms)
const int kAnimationDurationSlow = 500;

/// Very slow animation duration (1000ms)
const int kAnimationDurationVerySlow = 1000;

// ============================================================================
// SCREEN BREAKPOINT CONSTANTS
// ============================================================================

/// Mobile breakpoint (max width)
const double kBreakpointMobile = 600.0;

/// Tablet breakpoint (min width)
const double kBreakpointTablet = 600.0;

/// Desktop breakpoint (min width)
const double kBreakpointDesktop = 1200.0;

// ============================================================================
// SPECIFIC UI ELEMENT CONSTANTS
// ============================================================================

/// Timer bar height
const double kTimerBarHeight = 20.0;

/// Life star spacing
const double kLifeStarSpacing = 0.0;

/// Countdown circle size
const double kCountdownCircleSize = 140.0;

/// Countdown circle inner size
const double kCountdownCircleInnerSize = 120.0;

/// Progress indicator stroke width
const double kProgressStrokeWidth = 8.0;

/// Minimum screen width
const double kMinScreenWidth = 600.0;
